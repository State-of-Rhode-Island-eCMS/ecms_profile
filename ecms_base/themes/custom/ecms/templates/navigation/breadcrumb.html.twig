{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}

{{ include('ecms:breadcrumb', {
  breadcrumb: breadcrumb
}, with_context = FALSE) }}
