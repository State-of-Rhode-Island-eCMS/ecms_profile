{#
/**
 * @file
 * Theme override for the ecms hotel listing block.
#}

{{ include('ecms:hotels-grid', {
  label: label,
  cards: content,
  title_prefix: title_prefix,
  title_suffix: title_suffix,
}, with_context = TRUE) }}
