langcode: en
status: true
dependencies:
  config:
    - user.role.drupal_administrator
  module:
    - user
id: user_remove_role_action.drupal_administrator
label: 'Remove the Drupal Administrator role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: drupal_administrator
