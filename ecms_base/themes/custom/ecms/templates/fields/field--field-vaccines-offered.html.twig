{#
/**
 * @file
 * A blank template which returns field data without a wrapper.
 *
 * Available variables:
 * - label: The label for the field.
 * - items: List of all the field items. Each item contains:
 *   - attributes: List of HTML attributes for each item.
 *   - content: The field item's content.
 */
#}

{% include '@molecules/vaccines-offered/vaccines-offered.twig' with {
  label: label,
  items: items
} %}
