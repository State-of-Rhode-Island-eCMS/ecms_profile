name: eCMS GTranslate
type: eCMS
description: "Configures the GTranslate module for eCMS with custom settings and block placement"
install:
  - gtranslate
config:
  strict: false
  actions:
    gtranslate.settings:
      simpleConfigUpdate:
        widget_look: "lang_names"
